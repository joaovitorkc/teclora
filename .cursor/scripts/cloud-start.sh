#!/usr/bin/env bash
# Sem HTTP, Postgres, Redis ou Next. O painel só roda no Mac (Option+Space).
set -euo pipefail
echo "[start] Teclora não sobe servidor nesta VM"
echo "[start] ready — editar Swift; testar o .app no Mac"
exit 0
